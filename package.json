{
  "name": "performancetestjs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server --watch db.json --port 3001\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "concurrently": "^10.0.3",
    "json-server": "^1.0.0-beta.15",
    "tailwindcss": "^4.3.0"
  }
}
